name: sdrun-dev
channels:
    - defaults
dependencies:
    - python=3.6
    - numpy
    - glotzer::hoomd
    - pytest
    - pylint
    - setuptools
    - hypothesis
    - coverage
    - conda-forge::bumpversion
    - pip:
        - mypy
        - pytest-mypy
        - pytest-pylint
        - pytest-cov

